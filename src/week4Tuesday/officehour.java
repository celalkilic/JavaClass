package week4Tuesday;

import java.util.Scanner;

public class officehour {

	public static void main(String[] args, int x) {
		/*
		 * 1.soru Scanner scanner = new Scanner(System.in); String word =
		 * scanner.next(); for (int i = 0; i < word.length(); i+=2) {
		 * System.out.print(word.charAt(i)); }
		 */
		/*
		 * 2.soru Scanner scanner = new Scanner(System.in); String word =
		 * scanner.next(); String newword = ""; char [] vowels =
		 * {'A','E','O','U','a','e','o','u','I','i'}; for (char c :word.toCharArray() )
		 * { for (char d : vowels) { if (c==d) { newword+=c; } } }
		 * System.out.print(newword);
		 */
		/*
		 * 3.soru Scanner scanner = new Scanner(System.in); String word =
		 * scanner.nextLine(); String newword = ""; for (int i = 0; i < word.length();
		 * i++) { if (i%2==0) { newword+=word.substring(i, i+1).toLowerCase();
		 * 
		 * }else { newword+=word.substring(i, i+1).toUpperCase(); } }
		 * System.out.println(newword);
		 */
		/*
		 * 4.soru Scanner scanner =new Scanner(System.in); String reverse =
		 * scanner.nextLine(); for (int i = reverse.length()-1; i>=0 ; i--) {
		 * System.out.print(reverse.charAt(i));
		 * 
		 * }
		 */
		/*
		 * 5.soru Scanner scanner = new Scanner(System.in); String wordString =
		 * scanner.nextLine(); 
		 * String newwordString = ""; 
		 * for (int i =wordString.length()-1;i>=0; i--)
		 * { newwordString+= wordString.charAt(i); }
		 * System.out.println(newwordString);
		 * System.out.println(newwordString.equalsIgnoreCase(wordString));
		 */
		

	}
}
